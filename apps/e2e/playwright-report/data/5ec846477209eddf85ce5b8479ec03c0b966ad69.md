# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: notifications.spec.ts >> Notifications Flow >> User can send an SMS notification
- Location: tests/notifications.spec.ts:33:3

# Error details

```
Test timeout of 30000ms exceeded while running "beforeEach" hook.
```

```
Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
Call log:
  - navigating to "http://localhost:3001/login", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Notifications Flow', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     // Signup to get a session
> 6  |     await page.goto('/login', { timeout: 60000 });
     |                ^ Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
  7  |     
  8  |     const nameInput = page.locator('input[name="name"]');
  9  |     await nameInput.waitFor({ state: 'visible' });
  10 | 
  11 |     await nameInput.fill('Notify User');
  12 |     await page.fill('input[name="email"]', `notify-${Date.now()}@example.com`);
  13 |     await page.fill('input[name="password"]', 'Password123!');
  14 |     
  15 |     await Promise.all([
  16 |       page.waitForURL(/\/dashboard/, { timeout: 15000 }),
  17 |       page.click('button[type="submit"]:has-text("Sign Up")')
  18 |     ]);
  19 |   });
  20 | 
  21 |   test('User can send an email notification', async ({ page }) => {
  22 |     // Fill Send Notification form
  23 |     await page.fill('input[placeholder="email@example.com"]', 'recipient@example.com');
  24 |     await page.fill('input[placeholder="Assunto do email"]', 'Test Subject');
  25 |     await page.fill('textarea[placeholder="Digite a mensagem..."]', 'Hello from E2E test!');
  26 |     
  27 |     await page.click('button:has-text("Enviar")');
  28 |     
  29 |     // Check toast
  30 |     await expect(page.locator('text=Notificação enviada com sucesso!')).toBeVisible();
  31 |   });
  32 | 
  33 |   test('User can send an SMS notification', async ({ page }) => {
  34 |     // Change type to SMS
  35 |     await page.click('button:has([data-placeholder="Select a type"], :text("Email"))'); 
  36 |     await page.click('text=SMS');
  37 |     
  38 |     await page.fill('input[placeholder="+5511999999999"]', '+1234567890');
  39 |     await page.fill('textarea[placeholder="Digite a mensagem..."]', 'SMS from E2E test!');
  40 |     
  41 |     await page.click('button:has-text("Enviar")');
  42 |     
  43 |     // Check toast
  44 |     await expect(page.locator('text=Notificação enviada com sucesso!')).toBeVisible();
  45 |   });
  46 | });
  47 | 
```